services:
  mysql:
    image: mysql:8.4
    container_name: go-api-jwt-mysql

    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: go_api_jwt

    ports:
      - "3306:3306"

    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data:
